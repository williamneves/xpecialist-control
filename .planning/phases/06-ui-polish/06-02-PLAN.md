---
phase: 06-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/DraftDetailDialog.tsx
  - src/components/DraftDetailSheet.tsx
  - src/routes/index.tsx
autonomous: true

must_haves:
  truths:
    - "DraftDetailSheet replaced with DraftDetailDialog (centered modal)"
    - "Approve button text simplified to 'Aprovar' (no wrapping)"
    - "All existing sheet functionality preserved in dialog"
  artifacts:
    - path: "src/components/DraftDetailDialog.tsx"
      provides: "Centered modal for draft details"
      min_lines: 400
    - path: "src/routes/index.tsx"
      provides: "Dashboard using new Dialog component"
      contains: "DraftDetailDialog"
  key_links:
    - from: "src/routes/index.tsx"
      to: "src/components/DraftDetailDialog.tsx"
      via: "Component import and usage"
      pattern: "import.*DraftDetailDialog"
---

<objective>
Replace the side-sliding DraftDetailSheet with a centered DraftDetailDialog modal and simplify button text.

Purpose: Centered dialogs provide better focus for review tasks; simplified button text prevents wrapping
Output: New DraftDetailDialog component replacing Sheet, simplified "Aprovar" button text
</objective>

<execution_context>
@/home/clawdbot/.claude/get-shit-done/workflows/execute-plan.md
@/home/clawdbot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ui-polish/06-RESEARCH.md
@src/components/DraftDetailSheet.tsx
@src/components/ui/dialog.tsx
@src/routes/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DraftDetailDialog component from Sheet</name>
  <files>src/components/DraftDetailDialog.tsx</files>
  <action>
    Create new file by converting DraftDetailSheet to use Dialog instead of Sheet:

    1. Copy DraftDetailSheet.tsx to DraftDetailDialog.tsx

    2. Update imports - replace Sheet imports with Dialog imports:
       ```typescript
       import {
         Dialog,
         DialogContent,
         DialogDescription,
         DialogFooter,
         DialogHeader,
         DialogTitle,
       } from "@/components/ui/dialog";
       ```
       Remove Sheet imports.

    3. Rename component from DraftDetailSheet to DraftDetailDialog

    4. Replace JSX structure:
       - `<Sheet>` → `<Dialog>`
       - `<SheetContent side="right" className="...">` → `<DialogContent className="max-w-2xl overflow-y-auto max-h-[90vh] ...">`
         Keep the armed state ring classes: `isArmed && "ring-2 ring-inset ring-[#1DA1F2]"` etc.
       - `<SheetHeader>` → `<DialogHeader>`
       - `<SheetTitle>` → `<DialogTitle>`
       - `<SheetDescription>` → `<DialogDescription>`
       - `<SheetFooter>` → `<DialogFooter>`

    5. Simplify approve button text:
       Change: `{isApproving ? "Aprovando..." : "Segurar para Aprovar"}`
       To: `{isApproving ? "Aprovando..." : "Aprovar"}`

       This fixes the button text wrapping issue (closes button-wrapping-drawer todo).

    6. Remove the closing `</Sheet>` tag and replace with `</Dialog>`

    7. Keep all existing functionality: mutations, state management, forms, ScheduleDialog.
  </action>
  <verify>
    File exists at src/components/DraftDetailDialog.tsx with Dialog-based structure
  </verify>
  <done>
    DraftDetailDialog.tsx created using Dialog component, button text simplified to "Aprovar"
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Dashboard to use DraftDetailDialog</name>
  <files>src/routes/index.tsx</files>
  <action>
    Replace DraftDetailSheet with DraftDetailDialog in the Dashboard:

    1. Update import:
       Change: `import DraftDetailSheet from "@/components/DraftDetailSheet";`
       To: `import DraftDetailDialog from "@/components/DraftDetailDialog";`

    2. Update component usage (near end of Dashboard JSX):
       Change: `<DraftDetailSheet ... />`
       To: `<DraftDetailDialog ... />`
       Keep all props exactly the same:
       - draft={selectedDraft}
       - open={sheetOpen}
       - onOpenChange={handleSheetClose}
       - onActionComplete={handleActionComplete}
       - isArmed={isArmed}
       - isRejectArmed={isRejectArmed}
       - initialShowRejectForm={showRejectForm}
       - initialEditMode={startEditMode}

    3. Optionally rename state variables for clarity (but not required):
       - sheetOpen → dialogOpen
       - handleSheetClose → handleDialogClose
       (Skip this if it adds unnecessary churn - the names still work)
  </action>
  <verify>
    `bun --bun run build` passes, no import errors
  </verify>
  <done>
    Dashboard imports and uses DraftDetailDialog instead of DraftDetailSheet
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean up old Sheet component (optional)</name>
  <files>src/components/DraftDetailSheet.tsx</files>
  <action>
    Keep DraftDetailSheet.tsx for now (don't delete).

    Reason: The file can be safely removed later after verification. For now, keeping it
    allows easy rollback if issues are found. Add a comment at the top:

    ```typescript
    // DEPRECATED: Replaced by DraftDetailDialog.tsx
    // Safe to delete after Phase 6 verification
    ```

    This is a non-blocking action - the old file is not imported anywhere.
  </action>
  <verify>
    Comment added to DraftDetailSheet.tsx
  </verify>
  <done>
    Old Sheet component marked as deprecated, ready for cleanup post-verification
  </done>
</task>

</tasks>

<verification>
1. Run `bun --bun run build` - no TypeScript errors
2. Run `bun --bun run check` - no lint errors
3. Test draft detail opening - should appear as centered modal, not side sheet
4. Test approve button - text should be "Aprovar" (no wrapping)
5. Test all existing functionality: approve, reject, edit, schedule
6. Verify armed state visuals still work (blue/red rings)
</verification>

<success_criteria>
- DraftDetailDialog.tsx exists and uses Dialog component
- Dashboard imports DraftDetailDialog, not Sheet
- Approve button shows "Aprovar" not "Segurar para Aprovar"
- Modal appears centered instead of sliding from right
- All existing functionality works: mutations, forms, armed states
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-polish/06-02-SUMMARY.md`
</output>
