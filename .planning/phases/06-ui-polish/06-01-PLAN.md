---
phase: 06-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/index.tsx
  - src/styles.css
autonomous: true

must_haves:
  truths:
    - "Dashboard container is centered with max-w-5xl and mx-auto"
    - "Dark mode has improved contrast (muted-foreground brighter, borders more visible)"
    - "Auth loading state shows proper skeleton matching page layout"
  artifacts:
    - path: "src/routes/index.tsx"
      provides: "Centered layout container, auth loading skeleton"
      contains: "max-w-5xl mx-auto"
    - path: "src/styles.css"
      provides: "Improved dark mode contrast variables"
      contains: "muted-foreground.*0.75"
  key_links:
    - from: "src/routes/index.tsx"
      to: "SignedIn/SignedOut"
      via: "Conditional rendering with auth loading"
      pattern: "isLoaded.*Skeleton"
---

<objective>
Improve dashboard layout and visual polish: centered container with max-width, enhanced dark mode contrast, and proper auth loading skeleton.

Purpose: Addresses layout-centralizado todo and improves visual experience in dark mode
Output: Polished layout with better contrast and loading states
</objective>

<execution_context>
@/home/clawdbot/.claude/get-shit-done/workflows/execute-plan.md
@/home/clawdbot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ui-polish/06-RESEARCH.md
@src/routes/index.tsx
@src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard layout to centered max-w-5xl</name>
  <files>src/routes/index.tsx</files>
  <action>
    Update the container classes in Dashboard component:
    1. Replace `max-w-screen-2xl` with `max-w-5xl mx-auto` in both SignedIn and SignedOut container divs
    2. Keep responsive padding `py-8 px-4`
    3. The container should now be: `container max-w-5xl mx-auto py-8 px-4`

    This provides a more readable content width (1024px max) centered on large screens.
  </action>
  <verify>
    Visual check: content no longer spans full width on large screens, is centered
  </verify>
  <done>
    Both SignedIn and SignedOut sections use `max-w-5xl mx-auto` for centered layout
  </done>
</task>

<task type="auto">
  <name>Task 2: Add auth loading skeleton state</name>
  <files>src/routes/index.tsx</files>
  <action>
    Import useAuth from @clerk/clerk-react and add loading state handling:

    1. Add import: `import { useAuth } from "@clerk/clerk-react"`
    2. At the start of Dashboard function, add:
       ```typescript
       const { isLoaded } = useAuth();
       ```
    3. Before the SignedIn/SignedOut block, add loading state check:
       ```typescript
       if (!isLoaded) {
         return (
           <div className="container max-w-5xl mx-auto py-8 px-4">
             <div className="flex items-center justify-between mb-8">
               <div>
                 <Skeleton className="h-9 w-48 mb-2" />
                 <Skeleton className="h-5 w-72" />
               </div>
               <Skeleton className="h-9 w-24" />
             </div>
             <Skeleton className="h-96 w-full rounded-lg" />
           </div>
         );
       }
       ```

    This shows a skeleton matching the dashboard layout while Clerk auth loads.
  </action>
  <verify>
    Visual check: on hard refresh, skeleton appears briefly before auth state resolves
  </verify>
  <done>
    Loading skeleton shows while Clerk isLoaded is false, matching dashboard layout structure
  </done>
</task>

<task type="auto">
  <name>Task 3: Improve dark mode contrast in CSS variables</name>
  <files>src/styles.css</files>
  <action>
    Update the .dark section CSS variables for better contrast:

    1. Increase muted-foreground brightness for more readable muted text:
       Change: `--muted-foreground: oklch(0.705 0.015 286.067);`
       To: `--muted-foreground: oklch(0.75 0.015 286.067);`

    2. Increase border visibility:
       Change: `--border: oklch(0.274 0.006 286.033);`
       To: `--border: oklch(0.35 0.006 286.033);`

    3. Make card background slightly lighter for better separation:
       Change: `--card: oklch(0.141 0.005 285.823);`
       To: `--card: oklch(0.18 0.005 285.823);`

    These changes improve WCAG contrast ratios and reduce eye strain in dark mode.
  </action>
  <verify>
    Visual check in dark mode: muted text more readable, borders more visible, cards distinct from background
  </verify>
  <done>
    Dark mode CSS variables updated with improved contrast values
  </done>
</task>

</tasks>

<verification>
1. Run `bun --bun run build` - no TypeScript errors
2. Run dev server, check dashboard in both light and dark mode
3. Verify container is centered (not full width) on wide screens
4. Verify loading skeleton appears on hard refresh
5. Verify dark mode has improved text/border contrast
</verification>

<success_criteria>
- Dashboard container uses `max-w-5xl mx-auto` class
- Auth loading state shows skeleton before Clerk resolves
- Dark mode muted-foreground is brighter (0.75 vs 0.705)
- Dark mode borders are more visible (0.35 vs 0.274)
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-polish/06-01-SUMMARY.md`
</output>
