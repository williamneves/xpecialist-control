---
phase: 04-keyboard-shortcuts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/KeyboardHelpDialog.tsx
autonomous: true

must_haves:
  truths:
    - "Help dialog displays all keyboard shortcuts in a flat list"
    - "Dialog is accessible and closes with Escape"
  artifacts:
    - path: "src/components/KeyboardHelpDialog.tsx"
      provides: "Help modal component for keyboard shortcuts"
      exports: ["KeyboardHelpDialog"]
      min_lines: 40
  key_links:
    - from: "src/components/KeyboardHelpDialog.tsx"
      to: "@/components/ui/dialog"
      via: "Radix Dialog import"
      pattern: "import.*Dialog.*from.*@/components/ui/dialog"
---

<objective>
Create the KeyboardHelpDialog component that displays all available keyboard shortcuts.

Purpose: Discoverability for keyboard shortcuts via ? key.
Output: Reusable help dialog component ready for integration.
</objective>

<execution_context>
@/home/clawdbot/.claude/get-shit-done/workflows/execute-plan.md
@/home/clawdbot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-keyboard-shortcuts/04-CONTEXT.md
@src/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KeyboardHelpDialog component</name>
  <files>src/components/KeyboardHelpDialog.tsx</files>
  <action>
Create `src/components/KeyboardHelpDialog.tsx`:

**Props:**
```typescript
interface KeyboardHelpDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
}
```

**Content:**
- Title: "Atalhos de Teclado" (Portuguese, consistent with app)
- Simple flat list of shortcuts with kbd styling:

| Key | Action |
|-----|--------|
| Alt+A (2x) | Aprovar |
| Alt+R (2x) | Rejeitar rapidamente |
| Alt+R + Alt+Y | Rejeitar com motivo |
| Alt+E | Editar |
| J | Proximo draft |
| K | Draft anterior |
| ? | Ajuda |
| Esc | Fechar |

**Note on reject shortcuts:**
- Alt+R (2x) means: press Alt+R to arm, then Alt+R again to confirm quick-reject
- Alt+R + Alt+Y means: press Alt+R to arm, then Alt+Y to open reject dialog for custom reason

**Styling:**
- Use existing Dialog component from @/components/ui/dialog
- kbd elements with muted background and border (Tailwind: `bg-muted px-2 py-1 rounded border text-xs font-mono`)
- Clean, simple layout - no icons needed
- Dialog max-width around sm or md

**Accessibility:**
- Dialog handles Escape key and focus trap automatically via Radix
- Descriptive DialogDescription for screen readers
  </action>
  <verify>
TypeScript compiles: `bun run check` passes
Component renders without errors when imported
  </verify>
  <done>
KeyboardHelpDialog exists at src/components/KeyboardHelpDialog.tsx with:
- Controlled open/onOpenChange props
- All shortcuts listed in a flat, readable format
- Correct reject shortcuts: Alt+R (2x) for quick, Alt+R + Alt+Y for custom reason
- Uses existing Dialog component
- Portuguese labels consistent with app
  </done>
</task>

</tasks>

<verification>
- [ ] `bun run check` passes
- [ ] `bun run build` succeeds
- [ ] Component exports KeyboardHelpDialog
- [ ] Uses Radix Dialog from existing ui components
- [ ] Shows updated reject shortcut scheme (chord pattern)
</verification>

<success_criteria>
- KeyboardHelpDialog component created
- Lists all keyboard shortcuts with clear formatting
- Reject shortcuts show the armed + chord pattern correctly
- Ready for integration with useKeyboardShortcuts hook
</success_criteria>

<output>
After completion, create `.planning/phases/04-keyboard-shortcuts/04-02-SUMMARY.md`
</output>
