---
phase: 03-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/calendar.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/datetime-picker.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "DateTimePicker component renders calendar with date selection"
    - "DateTimePicker allows hour and minute selection via dropdowns"
    - "Selected date/time reflects in component state"
  artifacts:
    - path: "src/components/ui/calendar.tsx"
      provides: "shadcn Calendar component"
      min_lines: 30
    - path: "src/components/ui/popover.tsx"
      provides: "shadcn Popover component"
      min_lines: 20
    - path: "src/components/ui/datetime-picker.tsx"
      provides: "Combined date + time picker"
      min_lines: 60
      exports: ["DateTimePicker"]
  key_links:
    - from: "src/components/ui/datetime-picker.tsx"
      to: "src/components/ui/calendar.tsx"
      via: "import Calendar"
      pattern: "import.*Calendar.*from.*calendar"
    - from: "src/components/ui/datetime-picker.tsx"
      to: "date-fns"
      via: "date manipulation"
      pattern: "import.*from.*date-fns"
---

<objective>
Add date/time picker dependencies and create DateTimePicker component for scheduling drafts.

Purpose: Provide the foundational UI component for scheduling - a combined date picker (calendar) with time selection (hour/minute dropdowns).
Output: DateTimePicker component ready for use in ScheduleDialog.
</objective>

<execution_context>
@/home/clawdbot/.claude/get-shit-done/workflows/execute-plan.md
@/home/clawdbot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduling/03-RESEARCH.md

# Existing UI patterns
@src/components/ui/select.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn Calendar and Popover components + date-fns</name>
  <files>
    src/components/ui/calendar.tsx
    src/components/ui/popover.tsx
    package.json
  </files>
  <action>
Install dependencies and add shadcn components:

1. Run: `pnpm dlx shadcn@latest add calendar popover`
   - This adds react-day-picker dependency and creates calendar.tsx + popover.tsx

2. Run: `bun add date-fns`
   - date-fns 4.x for date manipulation and formatting

Verify components are created in src/components/ui/ directory.
Note: shadcn CLI will handle all the Radix UI primitive dependencies.
  </action>
  <verify>
    - Files exist: src/components/ui/calendar.tsx, src/components/ui/popover.tsx
    - `grep -q "react-day-picker" package.json` returns success
    - `grep -q "date-fns" package.json` returns success
    - `bun run check` passes (no lint errors in new files)
  </verify>
  <done>
    - Calendar and Popover components installed
    - date-fns and react-day-picker in package.json
    - No lint/type errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DateTimePicker component</name>
  <files>src/components/ui/datetime-picker.tsx</files>
  <action>
Create a DateTimePicker component that combines:
- Calendar for date selection (via Popover)
- Hour dropdown (0-23, padded to 2 digits)
- Minute dropdown (0, 15, 30, 45 - 15-minute intervals)

Component props:
```typescript
interface DateTimePickerProps {
  value: Date | undefined;
  onChange: (date: Date | undefined) => void;
  minDate?: Date;
  disabled?: boolean;
}
```

Implementation requirements:
1. Use shadcn Popover to wrap Calendar
2. Display selected date in button (format: "PPP" from date-fns, e.g., "Feb 3, 2026")
3. When date changes, preserve existing time (or default to 9:00 AM if no time set)
4. Time dropdowns disabled until date is selected (avoid Pitfall 5 from research)
5. Calendar disabled prop: dates before minDate should be unselectable
6. Show CalendarIcon from lucide-react in date trigger button

Use date-fns functions:
- `format` for display
- `setHours`, `setMinutes` for time manipulation

Follow existing Select component patterns for time dropdowns.
Display timezone indicator below the picker: `{timezone} (GMT offset)` using Intl.DateTimeFormat.
  </action>
  <verify>
    - `bun run check` passes
    - Component exports DateTimePicker
    - TypeScript types compile without errors
  </verify>
  <done>
    - DateTimePicker component created with calendar + time dropdowns
    - Time controls disabled when no date selected
    - Timezone indicator displayed
    - Ready for use in ScheduleDialog
  </done>
</task>

</tasks>

<verification>
- `bun run check` passes
- `bun run build` completes without errors
- Calendar, Popover, and DateTimePicker components exist in src/components/ui/
- date-fns and react-day-picker in package.json dependencies
</verification>

<success_criteria>
1. DateTimePicker component can be imported and renders without errors
2. Date selection via calendar popover works
3. Time selection via dropdowns works (disabled until date selected)
4. Timezone displayed correctly based on user's browser
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling/03-01-SUMMARY.md`
</output>
