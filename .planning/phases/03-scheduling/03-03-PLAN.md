---
phase: 03-scheduling
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/routes/dashboard.tsx
autonomous: true

must_haves:
  truths:
    - "Scheduled tab shows drafts with status='scheduled'"
    - "Each scheduled draft displays its scheduled time in user timezone"
    - "Scheduled time formatted as readable date+time string"
  artifacts:
    - path: "src/routes/dashboard.tsx"
      provides: "Updated scheduled tab with time display"
      contains: "scheduledFor"
  key_links:
    - from: "src/routes/dashboard.tsx"
      to: "Intl.DateTimeFormat"
      via: "timezone formatting"
      pattern: "Intl\\.DateTimeFormat|formatScheduledTime"
---

<objective>
Display scheduled time in the scheduled drafts tab on the dashboard.

Purpose: Show users when their scheduled drafts will be published (SCHED-03).
Output: Scheduled tab displays scheduled time for each draft.
</objective>

<execution_context>
@/home/clawdbot/.claude/get-shit-done/workflows/execute-plan.md
@/home/clawdbot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduling/03-RESEARCH.md

# Dashboard where tabs are rendered
@src/routes/dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Display scheduled time in scheduled tab</name>
  <files>src/routes/dashboard.tsx</files>
  <action>
Update the scheduled tab in dashboard.tsx to display the scheduled time for each draft.

1. Check how the scheduled tab currently renders drafts (likely using listAll with status='scheduled').

2. Add or update a formatScheduledTime utility function:
   ```typescript
   function formatScheduledTime(timestamp: number): string {
     return new Intl.DateTimeFormat('pt-BR', {
       dateStyle: 'medium',
       timeStyle: 'short',
       timeZone: Intl.DateTimeFormat().resolvedOptions().timeZone,
     }).format(new Date(timestamp));
   }
   ```
   If a similar formatDate function exists, reuse it or ensure timezone is explicit.

3. In the table/list rendering for scheduled drafts:
   - Add a column or field showing "Agendado para: {formatted time}"
   - Use Calendar icon from lucide-react next to the time
   - Style: text-sm text-muted-foreground for the time

4. If using Table component:
   - Add TableHead: "Agendado para"
   - Add TableCell: `<div className="flex items-center gap-2"><Calendar className="h-4 w-4" />{formatScheduledTime(draft.scheduledFor!)}</div>`

5. If using Card view for scheduled tab:
   - Add scheduled time below content or in metadata area
   - Similar icon + formatted time pattern

The scheduled tab should clearly show WHEN each draft is scheduled for publication.
  </action>
  <verify>
    - `bun run check` passes
    - Scheduled tab displays formatted scheduled time for each draft
    - Time shown in user's local timezone (not UTC)
  </verify>
  <done>
    - Scheduled drafts tab shows scheduled time for each draft
    - Times displayed in user's local timezone
    - Format: "medium" date + "short" time (e.g., "3 de fev. de 2026, 14:30")
  </done>
</task>

</tasks>

<verification>
- `bun run check` passes
- `bun run build` completes without errors
- Scheduled tab shows scheduled time column/field
- Times are formatted in user's local timezone
</verification>

<success_criteria>
1. Navigate to scheduled tab
2. Each scheduled draft shows its scheduled publication time
3. Time is displayed in user's local timezone (not UTC)
4. Format is readable (date + time)
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling/03-03-SUMMARY.md`
</output>
