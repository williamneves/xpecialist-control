---
phase: 01-core-review-queue
plan: 06
type: execute
wave: 4
depends_on: ["01-03", "01-04", "01-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All Phase 1 success criteria verified manually"
    - "End-to-end flow works: auth -> view -> approve/reject -> advance"
    - "Thread grouping and bulk actions function correctly"
  artifacts: []
  key_links: []
---

<objective>
Human verification of complete Phase 1 functionality.

Purpose: Confirms all requirements (AUTH-01, AUTH-02, QUEUE-01-06, THRD-01-03, STAT-01-03) are satisfied before phase completion.

Output: Human-verified phase completion or list of issues to address.
</objective>

<execution_context>
@/home/clawdbot/.claude/get-shit-done/workflows/execute-plan.md
@/home/clawdbot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 1: Core Review Queue

Features implemented:
1. Auth protection on all routes (AUTH-01, AUTH-02)
2. Pending drafts list with thread grouping (QUEUE-01, THRD-01)
3. Draft content display with character count (QUEUE-02)
4. Hold-to-approve gesture (QUEUE-03)
5. Required rejection reason (QUEUE-04)
6. Auto-advance to next draft (QUEUE-05)
7. Empty state when no pending drafts (QUEUE-06)
8. Thread review as unit (THRD-02)
9. Bulk approve/reject for threads (THRD-03)
10. Status tabs (pending/approved/rejected/scheduled/published) (STAT-01, STAT-02, STAT-03)
  </what-built>
  <how-to-verify>
Start the dev server if not running:
```bash
bun --bun run dev
# In separate terminal:
npx convex dev
```

**Test 1: Authentication (AUTH-01, AUTH-02)**
1. Open http://localhost:3000 in incognito/private window
2. EXPECTED: See "Acesso Restrito" card with sign-in button
3. Click "Entrar com Clerk" and sign in
4. EXPECTED: Dashboard with drafts appears
5. Check Clerk UserButton in header shows your avatar

**Test 2: Pending Drafts View (QUEUE-01, QUEUE-02, QUEUE-06)**
1. With auth, view dashboard pending tab
2. EXPECTED: Drafts sorted newest-first
3. EXPECTED: Character counts visible (X/280)
4. If no pending drafts: EXPECTED: "Nenhum draft pendente" message

**Test 3: Approve with Hold Gesture (QUEUE-03)**
1. Click a single draft to open detail sheet
2. Try quick-clicking "Segurar para Aprovar"
3. EXPECTED: Nothing happens (too fast)
4. Hold the button for ~0.5 seconds
5. EXPECTED: Progress bar fills, then draft approved
6. EXPECTED: Toast "Draft aprovado!"

**Test 4: Reject with Required Reason (QUEUE-04)**
1. Click a pending draft
2. Click "Rejeitar"
3. Leave reason empty, click "Confirmar Rejeicao"
4. EXPECTED: Error "Motivo da rejeicao e obrigatorio"
5. Type "too short" (9 chars)
6. EXPECTED: Button still disabled or error about 10 chars
7. Type valid reason (10+ chars)
8. EXPECTED: Button enabled, rejection works
9. Check draft appears in Rejected tab with reason visible

**Test 5: Auto-Advance (QUEUE-05)**
1. Have 2+ pending drafts
2. Open first draft
3. Approve it (hold button)
4. EXPECTED: Next draft auto-opens in sheet
5. Approve until no more pending
6. EXPECTED: Sheet closes, toast "Todos os drafts pendentes foram revisados!"

**Test 6: Thread Grouping (THRD-01, THRD-02, THRD-03)**
(Requires thread data - create test data if needed)
1. Create 2-3 drafts with same metadata.threadId
2. View pending tab
3. EXPECTED: Thread shows as expandable group "Thread (X tweets)"
4. Expand to see all tweets in order
5. Click "Segurar para Aprovar" on thread
6. EXPECTED: All tweets in thread approved together
7. Test reject: requires reason, applies to all tweets

**Test 7: Status Tabs (STAT-01, STAT-02, STAT-03)**
1. Click each tab: Pendentes, Aprovados, Rejeitados, Agendados, Publicados
2. EXPECTED: Each tab shows drafts with that status
3. EXPECTED: Rejected tab shows rejection reasons
4. EXPECTED: Scheduled tab shows scheduled times (if any)
5. EXPECTED: Published tab shows published times (if any)
  </how-to-verify>
  <resume-signal>
Type "approved" if all tests pass.
Or describe specific issues found for gap closure.
  </resume-signal>
</task>

</tasks>

<verification>
Human verification of all Phase 1 success criteria.
</verification>

<success_criteria>
All 7 Phase 1 success criteria from ROADMAP.md verified:
1. User must authenticate via Clerk before accessing dashboard
2. User sees pending drafts listed newest-first with character counts
3. User can approve a draft with confirmation gesture
4. User can reject a draft with required reason
5. After approve/reject action, user auto-advances to next pending draft
6. Threads display as grouped units and approve/reject actions apply to entire thread
7. User can filter drafts by status (pending/approved/rejected/scheduled/published) via tabs
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-review-queue/01-06-SUMMARY.md`
</output>
